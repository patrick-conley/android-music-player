<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pconley.vamp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="pconley.vamp.library.LibraryActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="pconley.vamp.player.PlayerActivity"
            android:label="@string/activity_player_name"
            android:launchMode="singleTop"
            android:parentActivityName="pconley.vamp.library.LibraryActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pconley.vamp.LibraryActivity" />
        </activity>

        <receiver android:name=".player.AudioNoisyReceiver" >
            <intent-filter>
                <action android:name="android.media.AUDIO_BECOMING_NOISY" />
            </intent-filter>
        </receiver>

        <service android:name=".player.PlayerService" />
        <service android:name=".scanner.ScannerService" />

        <activity
            android:name=".preferences.SettingsActivity"
            android:label="@string/activity_settings_name" >
        </activity>
    </application>

</manifest>
